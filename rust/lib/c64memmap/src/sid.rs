pub struct SID {
  
}

impl SID {
  pub fn new() -> SID {
    return SID {

    };
  }

  pub fn get_byte(&self, addr: u16) -> u8 {
    return 0;
  }

  pub fn set_byte(&mut self, addr: u16, value: u8) {
    
  }
}